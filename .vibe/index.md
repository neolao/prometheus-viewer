# prometheus-viewer — Codebase index
> Generated by /vibe:sync on 2026-07-23. Re-run to update.

## Modules
- [`modules/app.md`](modules/app.md) — entry point and root UI shell of the SPA, gates the metrics view behind an optional login step
- [`modules/auth.md`](modules/auth.md) — collects optional username/password for Prometheus HTTP Basic Auth, in-memory only
- [`modules/api-prometheus.md`](modules/api-prometheus.md) — HTTP client for the Prometheus HTTP API
- [`modules/metrics.md`](modules/metrics.md) — fetches and displays the list of available Prometheus metrics

## Observed patterns
- React 19 functional components, no class components
- Vite + `@vitejs/plugin-react` for build/dev server
- Tests co-located with source (`*.test.tsx`/`*.test.ts`), Vitest with `jsdom` environment and Testing Library
- Feature UI components live under `src/features/<name>/`, backed by a plain HTTP client module under `src/api/`
- Async UI state modeled as a discriminated union (`{status: "loading" | "success" | "error", ...}`) rather than separate booleans
- Sensitive input (credentials) kept in component state only, never in env vars or browser storage — see `decisions/002-basic-auth-credentials-not-persisted.md`

## Other context files
- [`models.md`](models.md) — data models
- [`glossary.md`](glossary.md) — ubiquitous language
- [`decisions/`](decisions/) — architectural decision records
